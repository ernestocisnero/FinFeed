
export interface Iauth_initial_state {
    status: string,
    displayName: string | null,
    email: string | null,
    uid: string | null,
    photoURL: string | null,
    errorMessage: string | null,
}